/* @flow */
import { expect } from 'chai'
import configureStore from 'redux-mock-store'

import at from '../action-types'

describe('saveResource', function() {
  it('should set resource and trigger success notification', function() {
  })
});
